You are an expert planner, completing a policy written as symbolic rules. Your existing rules are good: every requirement holds in the **best case**. Some requirements still fail in the **worst case** because the agent can reach states that no rule covers.

{% include "_problem.md.j2" %}

{% include "_results.md.j2" %}

## Uncovered states where probability is lost

Share of the lost probability mass (time spent in a state × probability still at stake) for the failing requirements ({{ failing | join(', ') }}):
{% for h in hotspots %}
- {{ '%.0f' % (100 * h.mass) }}%: {{ h.state }}
{% endfor %}

## Task

Write **additional** rules for the uncovered states. They are appended after your existing rules, so they only apply where no existing rule matches, and they cannot change what the existing rules decide. General rules that cover whole regions are better than one rule per state. Return **only the new rules** as JSON: `{"rules": [{"condition": "...", "action": "..."}, ...]}`.
