## Your current rules

```
{{ rules_listing }}
```

The rules cover {{ reachable - uncovered }} of the {{ reachable }} reachable situations (combinations of the state variables where the action matters); {{ uncovered }} are uncovered.

## Verification results

| requirement | best case | worst case | needed | status |
|---|---|---|---|---|
{% for r in results %}
| `{{ r.name }}` | {{ '%.4f' % r.best }} | {{ '%.4f' % r.worst }} | {{ r.bound }} {{ r.threshold }} | {{ r.status }} |
{% endfor %}
